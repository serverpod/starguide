class: ChatSession
table: chat_session
fields:
  authUserId: UuidValue?
  keyToken: String
  goodAnswer: bool?
  createdAt: DateTime, default=now

indexes:
  createdAt:
    fields: createdAt
    unique: false